# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env                    # 실제 비밀/설정 값. .env.example만 커밋한다.

# --- Spark ---
# 스트리밍 체크포인트(offset/state 저장). 로컬 실행 시 생성되며 커밋하면 안 됨.
checkpoint/
spark/checkpoint/
*.crc
metastore_db/
derby.log

# --- Docker 볼륨 데이터 (로컬에 마운트되는 경우) ---
data/
pgdata/

# --- IDE / OS ---
.idea/
.vscode/
.DS_Store
